#include <bits/stdc++.h>

using namespace std;

int main() {
    // Your code starts here

    //outer loop same concept

    for (int i = 0; i < 5; i++)
    {
        // inner loop will check the condition for priniting start if i = 0 will print j 1 starr like that
        for (int j = 0; j < i ; j++)
        {
            /* code */

            cout << "*" ;
        }

        cout << endl;
        
    }
    
    
    return 0;
}